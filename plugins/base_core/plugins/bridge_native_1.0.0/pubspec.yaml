name: bridge_native
description: SON
version: 1.0.0

flutter:
  plugin:
    platforms:
      android:
        package: vn.app.son.bridge
        pluginClass: Pigeon
      ios:
        pluginClass: FLTPigeon

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  pigeon: ^10.1.4
  injectable_generator: any
  build_runner: any
  built_value_generator: any

environment:
  sdk: '>=3.3.2 <4.0.0'
