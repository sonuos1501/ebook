{
    "pageNumber": 123,
    "pageText": "## Hướng dẫn:\n**1. Trong tệp portfolio.html, đặt các ảnh vào cặp thẻ `<div></div>` như sau:**\n\n```html\n<div>\n  <img src=\"Images/Anh_1.jpg\">\n  <img src=\"Images/Anh_2.jpg\">\n</div>\n```\n\n**2. Các định kiểu từ bước này được đặt trong tệp style.css. Định kiểu cho thẻ `<div>` bằng cách khai báo vùng chọn là lớp image-style như sau:**\n\n```css\n.image-style {\n  background-color: rgb(51, 51, 51);\n  text-align: center;\n}\n```\n\n**Trong đó, thuộc tính `background-color` dùng để tạo nền màu tối và thuộc tính `text-align` dùng để căn giữa cho ảnh.**\n\n**3. Chỉ định tên lớp `image-style` cho thuộc tính `class` của thẻ `<div>`:**\n\n```html\n<div class=\"image-style\">\n  <img src=\"Images/Anh_1.jpg\">\n  <img src=\"Images/Anh_2.jpg\">\n</div>\n```\n\n**4. Vì các ảnh có kích thước lớn, có thể che mất phần nền, em thu nhỏ ảnh với khai báo vùng chọn kết hợp giữa lớp và thẻ như sau:**\n\n```css\n.image-style img {\n  padding: 20px;\n  max-height: 75%;\n  max-width: 75%;\n  border-radius: 40px;\n}\n```\n\n**Trong đó, vùng chọn `.image-style img` được dùng để định kiểu cho các thẻ `<img>` nằm trong thẻ `<div>` mà có thuộc tính `class` là `.image-style`. Nói cách khác, các thẻ `<img>` không thuộc thẻ `<div>` này sẽ không được định kiểu theo vùng chọn này.**\n\n**5. Nháy đúp chuột vào tệp `portfolio.html` để kiểm tra kết quả.**\n\n## VẬN DỤNG\n\n**Em hãy thực hiện các yêu cầu dưới đây.**\n\n1. **Định kiểu màu sắc tuỳ ý cho phần nền và phần văn bản của các danh sách `Sở thích` và `Các hoạt động đã tham gia` bằng vùng chọn tên lớp.**\n2. **Thay đổi định kiểu cho để mục `Lịch sinh hoạt hằng tuần` với các thuộc tính tuỳ ý bằng vùng chọn `id`.**\n3. **Định kiểu với màu sắc tuỳ ý để làm nổi bật một số ô trong bảng `Lịch sinh hoạt hằng tuần` bằng vùng chọn `id` hoặc tên lớp.**\n"
}